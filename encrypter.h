#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include <time.h>

int getCharCode(char c);
char* encrypt(char* str);
char** generateKey();
int indexOf(char** arr, int size, char* item);